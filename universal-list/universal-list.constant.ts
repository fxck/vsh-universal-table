export enum ModuleTokens {
  featureKey = 'universalList'
}

export enum ColumnTypes {
  String = 'string',
  Int = 'int',
  Float = 'float',
  Enum = 'enum',
  Datetime = 'datetime',
  Boolean = 'bool'
}

export const COLUMN_FILTER_NAMESPACE = 'column-filter';

export enum BaseUrls {
  stream = 'stream',
  clouddns = 'clouddns',
  cdn = 'cdn'
}
